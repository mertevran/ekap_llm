# --- Çekirdek (her zaman gerekli) ---
pydantic>=2.0
pydantic-settings>=2.0
httpx>=0.27
qdrant-client>=1.7

# --- Postgres arka ucu (yarın) ---
psycopg[binary]>=3.1

# --- Opsiyonel: sentence-transformers embedding arka ucu ---
# Sadece EMBEDDING_BACKEND=sentence-transformers kullanacaksanız gerekli (~2.2GB model indirir).
# Ollama arka ucu (varsayılan) bunlara ihtiyaç duymaz.
# sentence-transformers>=2.3
# torch>=2.0

# --- Geliştirme ---
pytest>=7.0

# NOT: `ollama` python paketi BİLEREK kullanılmıyor. Import anında global bir istemci
# kuruyor ve proxy'li ortamlarda import'u patlatıyor; ayrıca httpx zaten bağımlılığı.
# Ollama'nın REST API'sine doğrudan httpx ile gidiyoruz (app/embedding/embedders.py,
# app/decision/llm_client.py) — structured output (format=<json şeması>) dahil her
# özellik aynen kullanılabiliyor.
