[pytest]
minversion = 7.0
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers -p no:warnings
markers =
    external: gerçek servis gerektirir (Ollama, Postgres) — offline koşularda atlanır
